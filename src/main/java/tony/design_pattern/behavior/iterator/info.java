/**
 * 迭代器模式核心就是顺序访问聚集中的对象，而无需暴露集合对象的底层实现。
 * 迭代器模式是将迭代元素的责任交给迭代器，而不是聚合对象，
 * 在不知道该聚合对象的内部结构的情况下也能够实现对该聚合对象的迭代。
 *
 * java集合框架下的类基本都有对应的迭代器，就不作代码示例了
 */
package tony.design_pattern.behavior.iterator;
